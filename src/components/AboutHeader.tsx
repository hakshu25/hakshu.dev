import { HeaderBar } from './HeaderBar';
import { HeaderImage } from './HeaderImage';

export const AboutHeader = () => {
  return (
    <header>
      <HeaderBar />
      <HeaderImage />
    </header>
  );
};
