{
  "extends": ["stylelint-config-standard", "stylelint-config-prettier"],
  "rules": {
    "max-nesting-depth": 2,
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind"]
      }
    ]
  }
}
